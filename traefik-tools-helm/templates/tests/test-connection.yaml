apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "traefik-tools-helm.fullname" . }}-test-connection"
  labels:
    {{- include "traefik-tools-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "traefik-tools-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
